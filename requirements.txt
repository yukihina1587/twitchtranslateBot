# Twitch Chat Integration
twitchio>=2.9.0,<3.0.0

# HTTP Requests
requests>=2.31.0,<3.0.0
aiohttp>=3.9.0,<4.0.0

# Environment Variables
python-dotenv>=1.0.0,<2.0.0

# GUI Framework
customtkinter>=5.2.0,<6.0.0
Pillow>=10.0.0,<13.0.0

# Voice Recognition
SpeechRecognition>=3.10.0,<4.0.0
pyaudio>=0.2.13
websockets>=12.0  # WebSocket support for Gladia
# Note: Gladia SDK will be implemented using direct API calls via websockets

# Text-to-Speech (VOICEVOX + Fallback)
pygame>=2.5.0
pyttsx3>=2.90  # Fallback TTS engine when VOICEVOX is not available

# Resource Monitoring
psutil>=5.9.0,<6.0.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
